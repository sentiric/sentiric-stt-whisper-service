# 🧠 AI SYSTEM INSTRUCTIONS (SOP-00 COMPLIANCE)

Bu repoda kod yazan bir insan veya Yapay Zeka Ajanı (Cursor, Copilot vb.) AŞAĞIDAKİ KURALLARA KESİNLİKLE UYMAK ZORUNDADIR:

## 1. KÜTÜPHANE VE TEKNOLOJİ SINIRLARI
* **Motor:** `whisper.cpp` v1.8.2 kullanıyoruz. GGUF formata GEÇMEDİK. Modeller `.bin` formatındadır (GGML).
* **Dil:** Proje C++17 standartlarındadır. Python scriptleri KULLANILAMAZ.

## 2. KESİN YASAKLAR (DO NOTS)
* **VAD Yasaktır:** Whisper.cpp'nin VAD özelliği KAPATILMIŞTIR (`enable_vad = false`). "Smart Media, Dumb Logic" kuralı gereği söz kesme (Barge-in) işlemleri `stream-sdk` (Edge) tarafında yapılır. Çift VAD kullanımı yasaktır.
* **Diarization Yasaktır:** Whisper'ın yerleşik `tdrz` (tinydiarize) özelliği KARARSIZDIR VE KULLANILAMAZ. Biz kendi DSP tabanlı `SpeakerClusterer` algılamamızı yapıyoruz.
* **Standart Log Yasaktır:** `std::cout`, `printf` veya standart `spdlog` çağrıları YASAKTIR. Sadece `suts_logger.h` içindeki `SUTS_INFO`, `SUTS_ERROR`, `SUTS_WARN` makroları kullanılmalıdır. Bu makrolar `trace_id` ve `tenant_id` zorunluluğu ile SUTS v4.0 JSON formatını garanti eder.
* **Option/String Hatası:** gRPC metadata'dan okunan `trace_id`, `Some("...")` formatında OLMAMALIDIR. Loglara basılmadan önce saf string olarak unwrap edilmelidir.

## 3. HAFIZA VE PERFORMANS
* **Dynamic Batching:** `SttEngine`, GPU VRAM'ini korumak için `parallel_requests` sayısı kadar `whisper_state` havuzu kullanır (`std::mutex` ile korunur). Bu mekanizma BOZULAMAZ.